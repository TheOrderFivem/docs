# Version
The Version module checks for resource version updates on the server side.

## Server

Version checking runs automatically at startup. It compares the current resource version against the latest GitHub release and prints a notice in the server console if an update is available.

No public API functions are exposed — this module is used internally by Community Bridge.